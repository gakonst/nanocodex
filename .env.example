# Optional override for the pinned Terminal-Bench fix-git canary directory.
# HARBOR_CANARY_TASK=/absolute/path/to/task

# Optional replacement for the development command used by the Harbor adapter.
# HARNESS_COMMAND=cargo run --quiet -- run
