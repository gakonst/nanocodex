Audio routing patch: RemotePeer now configures iOS RTCAudioSession using VoicePeer exact API, playAndRecord/voiceChat/defaultToSpeaker/allowBluetooth/48k/10ms. No track before explicit opt-in. OS began interruption and oldDeviceUnavailable stop microphone and invoke onMicrophoneStopped (also invalidates pending permission). Observers removed on close/deinit. Mac/iOS permission descriptions updated. Focused lifecycle tests added without capture. REQUIRED integration: RemoteSession viewer should wire onMicrophoneStopped to stopMicrophone(notifyHost: true); root explicitly confirmed it owns this integration outside my RemotePeer-only source scope. Tests pending.

Commit 3da4d0a2. Focused Swift tests: 31 passed, zero failures, exit 0 (build/audio-route-tests.log and .exit). Repeated Mac/iOS simulator builds started detached after commit; logs build/audio-route-{macos,ios-simulator}-build.log and .exit. No capture or deployment. Root owns viewer callback integration.
