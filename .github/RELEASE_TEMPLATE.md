Install on Apple Silicon macOS or x86-64 glibc Linux:

```sh
curl -fsSL https://nanocodex.paradigm.xyz | bash
```

Existing installations can run `nanocodex update`. Every binary release asset
is covered by `SHA256SUMS`.
