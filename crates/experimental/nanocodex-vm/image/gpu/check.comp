#version 450
layout(local_size_x = 64) in;
layout(set = 0, binding = 0, std430) buffer Output { uint values[]; } result;
void main() {
    uint i = gl_GlobalInvocationID.x;
    result.values[i] = i * 3u + 7u;
}
