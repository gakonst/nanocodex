# Inference response completion — 2026-09-22

Passive generation observations now run through `waitUntil`, retaining the existing 250 ms timeout and error suppression. Synchronous registration failures are also caught, so an execution-context error cannot fail a completed generation. JSON delivery, stream EOF, and session release no longer await coordinator completion. The Worker ingress forwards its execution context through the inference API; Durable Object sessions use their own state context. Direct callers retain the Cloudflare `waitUntil` default. Tests can inject an execution context and await its background work.

For JSON requests, the projected SSE stream drains into a discard sink instead of `Response.arrayBuffer()`. Every frame and terminal response is still validated; the final response object is retained, but a second full encoded stream buffer is not. This does not make buffered providers incremental. Public buffering headers, stream projection, token timing, and cancellation propagation are unchanged.

Focused tests cover both stateless/session JSON and SSE responses completing with telemetry blocked, background work ending at 250 ms, sanitized provider failures, cancellation releasing session ownership with the pin intact, and strict stream parsing. Session tests also cover the combined pin/admission write and existing auth, restart, timeout, and tool replay behavior.

Validation uses pnpm 11.25.0 and the existing compiled Worker/WASM environment. Initial session run: 88/88 pass (`output/inference-streaming-runtime.log`, exit 0). The expanded run passes session 88/88 and stream 13/13; API 30/31 pass. Its sole failure is an existing model-catalog count assertion expecting 43 instead of 31 (`test/inference-api.test.ts:65`). That test, routing candidates, gateway availability, and catalog branch are unchanged from `45ccf07`; comparison evidence is in `output/inference-api-baseline-evidence.log`. Routing and the unrelated assertion were left unchanged.

Final focused runtime after registration hardening: 103/103 pass (90 session + 13 stream), 10.11 s total / 235 ms test execution, exit 0. Fresh managed typecheck passes with exit 0. Receipts are recorded in `output/inference-streaming-registration-runtime.*` and `output/inference-streaming-registration-typecheck.*`. The added JSON/SSE tests inject a throwing execution context after routing and a rejected observation write, then verify successful delivery, completed counters, and released session ownership. No live TTFT, memory, or end-to-end latency claim is made.

Follow-up baseline runtime check: isolated copies of `inference-api.ts`, `inference-session.ts`, and the API test from `git show 45ccf07` reproduced the exact catalog failure (expected 43, received 31). Only mutual import paths were rewritten; catalog/auth/key dependencies were verified unchanged. Only the real-key issuance test ran: 1 failed, 30 skipped, exit 1, 14.59 s total / 16 ms test execution. Receipts and source hashes are in `output/inference-api-baseline-runtime*`. All three created temporary source/test files were removed; production files were never swapped.

Rebase integration: upstream removed Sol/Luna Chat gateway routes while retaining their Responses routes. The existing REST pin-conflict fixture now selects a different valid Astra route, preserving its 409 check. Existing API catalog assertions were stale (including a four-effort Cloudflare count despite three supported efforts); they now assert 25 available entries, 34 with the Cloudflare gate, nine Cloudflare routes, and absence of the unsupported Sol/Luna Chat routes. Production routing policy is unchanged by this patch. Final post-rebase runtime receipts are recorded separately.

Final validation after rebasing onto `0371fa72545f15301072bf13f0665909fabe2cf1`: 159/159 tests pass across inference API, stateful sessions, stream parsing, idle discovery, and Session placement; exit 0 in 19.63 s. Both managed and account typechecks pass. The local fixture run used the validated `45ccf07` WASM artifact and freshly rebuilt TypeScript tools; deployment must use CI outputs built from the pushed revision. Receipts: `output/inference-rebased-final.*`, `output/inference-rebased-tools-build.*`, `output/inference-rebased-managed-types.*`, and `output/inference-rebased-account-types.*`.
