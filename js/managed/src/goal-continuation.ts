// Exact pinned source; rendering is shared with the canonical template adapter.
import { codexPrompt } from "./codex-prompts";
export const GOAL_CONTINUATION_TEMPLATE = codexPrompt("ext/goal/templates/goals/continuation.md");
