{
  "name": "nanocodex-managed2",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm prepare:just-bash && vitest run --config vitest.config.ts test/flow.test.ts test/bash-flow.test.ts",
    "test:websocket": "pnpm prepare:just-bash && MANAGED2_TEST_TRANSPORT=websocket vitest run --config vitest.config.ts test/relay-websocket.test.ts",
    "build": "pnpm prepare:just-bash && wrangler deploy --dry-run --outdir dist --config wrangler.jsonc",
    "prepare:just-bash": "node scripts/prepare-just-bash-lazy.mjs"
  },
  "dependencies": {
    "nanocodex": "workspace:*",
    "nanocodex-tools": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "^5.20260730.1",
    "esbuild": "0.28.1",
    "typescript": "5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "4.137.0"
  }
}
